# Check that the directory for an OM is valid

Check that the directory contains starter and forecast SS3 files.

## Usage

``` r
check_dir(dir)
```

## Arguments

- dir:

  Input to check. Should be a directory name that should contain an SS3
  model that can be used as an OM. @author Kathryn Doering
