# Put implementation error of 0 into a matrix

Put implementation error of 0 into a matrix

## Usage

``` r
get_impl_error_matrix(yrs)
```

## Arguments

- yrs:

  a vector of years

## Value

A length(yrs) row, 2 column matrix containing the years in the first
column and the implementation error values in the second.
